interface:
  display_name: "Subagent Delegation and Memory"
  short_description: "Delegate safely with durable context"
  default_prompt: "Use $subagent-memory to delegate this bounded task and decide whether it needs durable subordinate context."
